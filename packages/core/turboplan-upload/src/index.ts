/**
 * @wildfires-org/turboplan-upload
 *
 * Main entry point for the file upload package.
 *
 * For client-side usage:
 *   import { useFileUpload } from '@wildfires-org/turboplan-upload/client';
 *
 * For server-side usage:
 *   import { router } from '@wildfires-org/turboplan-upload/server';
 *
 * For type definitions:
 *   import type { UploadOptions, UploadResult } from '@wildfires-org/turboplan-upload/types';
 */

export * from "./types/index";
