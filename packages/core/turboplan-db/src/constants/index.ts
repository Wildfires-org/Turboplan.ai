/**
 * Database schema constants
 * These values define constraints used across database schemas
 */

/** Maximum length for URL slugs in database schemas */
export const MAX_SLUG_LENGTH = 40;

/** Maximum base slug length before adding unique suffix (-xxxxxx = 7 chars) */
export const MAX_BASE_SLUG_LENGTH = 32;
